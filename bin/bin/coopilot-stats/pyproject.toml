[project]
name = "coopilot-stats"
version = "0.1.0"
description = "A chart for Coopilot usage statistics"
requires-python = ">=3.11"
dependencies = [
    "matplotlib",
]

[tool.basedpyright]
include = ["chart.py"]
pythonVersion = "3.11"
